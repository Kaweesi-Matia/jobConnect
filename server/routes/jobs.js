import {Router} from 'express'; import {listJobs,getJob,createJob,myJobs,updateJob} from '../controllers/jobs.js'; import {protect,allow} from '../middleware/auth.js'; const r=Router(); r.get('/',listJobs); r.get('/mine',protect,allow('recruiter'),myJobs); r.get('/:id',getJob); r.post('/',protect,allow('recruiter'),createJob); r.patch('/:id',protect,allow('recruiter'),updateJob); export default r;
