import Job from '../models/Job.js';
export const listJobs=async(req,res)=>{try{const {q,type,location,experience,sort='newest',page=1,limit=12}=req.query;const filter={status:'open'};if(q)filter.$text={$search:q};if(type)filter.type=type;if(location)filter.location={$regex:location,'$options':'i'};if(experience)filter.experience=experience;const sortObj=sort==='salary' ? {salaryMax:-1}:sort==='oldest'?{createdAt:1}:{createdAt:-1};const skip=(Number(page)-1)*Number(limit);const [jobs,total]=await Promise.all([Job.find(filter).populate('recruiter','name company avatar').sort(sortObj).skip(skip).limit(Number(limit)),Job.countDocuments(filter)]);res.json({jobs,total,page:Number(page),pages:Math.ceil(total/limit)})}catch(e){res.status(500).json({message:e.message})}};
export const getJob=async(req,res)=>{const job=await Job.findById(req.params.id).populate('recruiter','name company website bio');if(!job)return res.status(404).json({message:'Job not found'});res.json(job)};
export const createJob=async(req,res)=>{try{const job=await Job.create({...req.body,recruiter:req.user._id,company:req.body.company||req.user.company||req.user.name});res.status(201).json(job)}catch(e){res.status(400).json({message:e.message})}};
export const myJobs=async(req,res)=>res.json(await Job.find({recruiter:req.user._id}).sort({createdAt:-1}));
export const updateJob=async(req,res)=>{const job=await Job.findOneAndUpdate({_id:req.params.id,recruiter:req.user._id},req.body,{new:true,runValidators:true});if(!job)return res.status(404).json({message:'Job not found'});res.json(job)};
