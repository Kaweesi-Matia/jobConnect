import{useEffect,useState}from'react';import{Search,SlidersHorizontal}from'lucide-react';import api from'../lib/api.js';import JobCard from'../components/JobCard.jsx';export default function Jobs(){const[jobs,setJobs]=useState([]),[q,setQ]=useState(''),[type,setType]=useState(''),[loading,setLoading]=useState(true);const load=async()=>{setLoading(true);const r=await api.get('/jobs',{params:{q,type}});setJobs(r.data.jobs);setLoading(false)};useEffect(()=>{load()},[type]);return <main className="page"><div className="jobs-heading"><div><p className="eyebrow">OPPORTUNITIES</p><h1>Find work that moves you forward.</h1><p>Search thousands of roles from teams hiring right now.</p></div></div><div className="searchbar"><Search/><input value={q} onChange={e=>setQ(e.target.value)} onKeyDown={e=>e.key==='Enter'&&load()} placeholder="Search by title, company or keyword"/><select value={type} onChange={e=>setType(e.target.value)}><option value="">All job types</option><option>Full-time</option><option>Part-time</option><option>Contract</option><option>Internship</option><option>Remote</option></select><button className="btn" onClick={load}><SlidersHorizontal size={16}/> Search</button></div><div className="results-head"><b>{jobs.length} open positions</b><span>Curated opportunities for ambitious people</span></div>{loading?<div className="loading">Loading opportunities…</div>:<div className="jobs-list">{jobs.map(j=><JobCard key={j._id} job={j}/>)}</div>}</main>}