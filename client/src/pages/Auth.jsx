import{useState}from'react';import{Link,useNavigate}from'react-router-dom';import{Briefcase,ArrowRight}from'lucide-react';import{useAuth}from'../context/AuthContext.jsx';export default function Auth({mode='login'}){const login=mode==='login';const{login:signin,register}=useAuth();const nav=useNavigate();const[role,setRole]=useState('candidate');const[form,setForm]=useState({name:'',email:'',password:''});const[error,setError]=useState('');const submit=async e=>{e.preventDefault();try{await(login?signin(form):register({...form,role}));nav(role==='recruiter'?'/recruiter':'/jobs')}catch(e){setError(e.response?.data?.message||'Something went wrong')}};return <div className="auth-page"><div className="auth-brand"><Link to="/" className="brand"><span className="logo"><Briefcase size={19}/></span>Job<span>Connect</span></Link><div className="auth-pitch"><p className="eyebrow">YOUR NEXT CHAPTER</p><h1>Build a career you’re proud of.</h1><p>Join a focused community where serious talent meets teams building what’s next.</p></div></div><form className="auth-card" onSubmit={submit}><h2>{login?'Welcome back':'Create your account'}</h2><p className="muted">{login?'Sign in to continue to JobConnect.':'Choose your account type and get started.'}</p>{!login&&<div className="role-switch"><button type="button" className={role==='candidate'?'active':''} onClick={()=>setRole('candidate')}>Candidate</button><button type="button" className={role==='recruiter'?'active':''} onClick={()=>setRole('recruiter')}>Recruiter</button></div>}{!login&&<label>Full name<input required value={form.name} onChange={e=>setForm({...form,name:e.target.value})} placeholder="Alex Morgan"/></label>}<label>Email address<input required type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})} placeholder="you@example.com"/></label><label>Password<input required minLength="6" type="password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})} placeholder="••••••••"/></label>{error&&<div className="error">{error}</div>}<button className="btn full">{login?'Sign in':'Create account'} <ArrowRight size={17}/></button><p className="switch">{login?<>New to JobConnect? <Link to="/register">Create an account</Link></>:<>Already have an account? <Link to="/login">Sign in</Link></>}</p></form></div>}